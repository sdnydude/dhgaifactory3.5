apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: false
    jsonData:
      maxLines: 1000
      # derivedFields (TraceID -> Tempo) removed 2026-09-04: Tempo retired
      # (AUDIT-2026-09 section 7, decision D-A).
