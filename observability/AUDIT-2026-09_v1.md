# Observability Audit — 2026-09-04

Phase 1 synthesis. Read-only audit by five Opus 5 agents, orchestrated by Fable 5.1. Raw agent reports are in `observability/audit-2026-09/` (1-architecture, 2-dashboards, 3-broken-pages, 4-pipeline-health, 5-gaps). Every claim below cites the report it came from as `[A1]`..`[A5]`. Items no agent could verify are collected in section 9. This document has not yet been through adversarial review.

Hosts: g700data1 = 10.0.0.251 (main stack). dh40801 = 10.0.0.179 (Langfuse v3).

---

## 1. Verdict

**Metrics and logs are current and healthy. Traces are dead. LLM-level observability does not exist. The two pages flagged as "error-filled" have no errors; they have query-logic and display defects.**

- Prometheus: 17 active targets, all UP, 0 scrape errors, fresh scrapes. [A4] (A1 and A3 counted 18; see 9.1.)
- Loki via Alloy: healthy, 6 redaction stages, ruler enabled. Promtail is gone; its config file is an orphan. [A1][A4]
- Tempo: **zero spans ever received** in 19.5 days of uptime. Not scraped by Prometheus. No alert. The only trace dashboard is 5/5 dead. [A1][A2][A4][A5]
- Langfuse: live and healthy on dh40801, **wired to nothing** on g700data1. No env keys on any running container, no Prometheus target, no Grafana datasource, no logs in Loki. [A1][A5]
- LangSmith: 22 `@traceable` import sites, 2 of them in the live registry API, all inert (no `LANGCHAIN_TRACING_V2` on any running container). [A1]
- Pydantic AI: one prototype file, not served, not containerised, not in `langgraph.json`. [A1][A5]
- Alerting: 18 Prometheus rules + 5 Loki rules. **3 Prometheus rules can never fire** (metric has no series). 2 Loki rules have been firing for 10 days on node-exporter log spam and produce no incident because they are `warning`. Webhook delivery to registry-api verified (38 POST 200). [A4][A5]

---

## 2. Scoreboard

| Layer | State | Consumer | Verdict |
|---|---|---|---|
| Metrics (Prometheus) | 17 targets UP | 9 dashboards, 18 rules | CURRENT, with defects (sections 4, 5) |
| Logs (Alloy to Loki) | healthy, 16 of 42 dhg-* containers emitted in 24h | dhg-log-analytics, 5 Loki rules | CURRENT; silent containers are app-side, not pipeline-side [A4] |
| Traces (OTel to Tempo) | 0 spans ever | dhg-langgraph-traces (empty) | DEAD, LangGraph-shaped [A1][A4] |
| LLM tracing (Langfuse) | server healthy | none | NOT WIRED [A1][A5] |
| Alerting (Alertmanager to registry webhook) | delivering | incident endpoint `registry/api.py:338` | CURRENT; 3 dead rules, 2 noisy rules [A4] |
| Cross-host (dh40801) | nothing | none | ABSENT [A5] |

---

## 3. Dashboard inventory [A2]

9 dashboards, not 6 as the prompt assumed. All 9 are provisioned from `observability/grafana/provisioning/dashboards/json/`, mount and provisioner path verified in-container, zero drift between repo JSON and live JSON on all 9.

| uid | Status | Evidence |
|---|---|---|
| dhg-postgresql | data on 14/14 panels; 4 display defects | section 4 |
| dhg-registry-api | 2 panels permanently No data; 8 panels doubled | section 4 |
| docker-overview | healthy 16/16 | — |
| dhg-log-analytics | healthy, 14 Loki panels | — |
| dhg-alerting | healthy 16/16; ALERTS panels empty because nothing fires | — |
| memreg-daemon | data present; sparse-histogram NaN gaps; **file untracked in git** | `git ls-files` shows 8 of 9 |
| dhg-core-golden | BROKEN 6/8 Prometheus targets dead | queries `registry_*_latency_ms_bucket` (real names have no `_ms`), `gpu_utilization`, `asr_*` (none exist) |
| vs-engine-overview | BROKEN 10/14 targets dead | 7 `vs_*` names queried that the exporter never publishes; only 5 `vs_*` names exist |
| dhg-langgraph-traces | BROKEN 5/5 | Tempo empty; pins `service.name="dhg-langgraph-agents"` |

Also: `observability/grafana/dashboards/` (dhg-core-golden.json, docker-overview.json) is a **dead tree**, not mounted, not referenced by compose; its docker-overview.json has invalid PromQL (`{image!""}`) on all 8 targets. Yet `.claude/commands/observability-engineer.md:48,326` still calls it the dashboard source. [A2]

MEMORY.md is wrong on two counts: "Mission Control" is a Next.js route, not a Grafana dashboard; "OTel to Tempo dual tracing" has never produced a span. [A2][A1]

---

## 4. The two flagged pages, panel by panel [A3]

Screenshots were not captured: `grafana-image-renderer` is not installed, and browser login could not be done without pasting the admin password into a tool argument (secret-safety rule). Findings come from replaying every panel through Grafana's own `/api/ds/query` plus Prometheus API and dashboard JSON. All 38 targets returned HTTP 200 with no error field. Pixel rendering is UNVERIFIED.

### dhg-registry-api (11 data panels)

Root cause of most of it: **the registry container is scraped twice**, by static job `registry-api` (`prometheus.yml:27-29`) and by `docker-sd` (`prometheus.yml:143-170`), with different relabels, so every series exists twice. Only panel 31 pins `job="registry-api"`. [A3] The same collision hits `dhg-vs-engine`. [A1][A3]

| id | title | class |
|---|---|---|
| 1 Request Rate, 6 Total Ops | **permanently No data**: `rate(write)+rate(read)` binary `+` on disjoint `operation` label sets, empty vector |
| 2 Error Rate | doubled; queries `registry_db_errors_total` (DB-conn only), the real `registry_errors_total` has 0 series and is never queried |
| 3 Write P95, 4 Read P95 | doubled; read histogram top finite bucket 1000ms with 0.47% overflow to +Inf, p99 unreliable |
| 5 DB Connections, 30 DB Pool | doubled; gauge 24h max = 1, not a pool |
| 10 Operations Rate | 50 series, static legend, 22 lines all named "Reads" |
| 20, 21 Latency p50/95/99 | 6 lines each for 3 intended |
| 31 Process Memory | fine (the only job-pinned panel) |
| row 100 "RED Metrics" | mislabelled: registry exposes **no HTTP metrics at all** (`registry/api.py:298` bare `generate_latest()`), so per-route rate/errors/duration cannot be measured by any dashboard edit |

Net visible effect: of six top-row tiles, 2 read "No data" and 4 render as two tiles crammed in one cell. That is what reads as "errors".

### dhg-postgresql (14 data panels)

No errors of any kind. 14/14 return data. Every metric exists, `datname` filter is valid, datasource uid correct, no gridPos overlaps.

| id | title | class |
|---|---|---|
| 1 Active Connections, 5 Longest TX | stat tile with 6 series (fan-out by `state`), needs `sum()` / `max()` |
| 20 Active Connections | 6 series, all legend-named "dhg_registry" |
| 11 Row Ops, 41 WAL and DB Size | mismatched magnitude on one linear axis (534/s vs 0.03/s; 80MiB vs 1.54GiB) |
| 40 Locks by Mode | 9 series all 0, informationally empty |
| 4 Deadlocks | raw counter, no `rate()` |
| 2, 3, 6, 10, 21, 30, 31 | fine |

---

## 5. Pipeline defects [A4][A1]

| # | Defect | Evidence |
|---|---|---|
| P1 | Double scrape of registry-api and vs-engine (static job + docker-sd) | `prometheus.yml:27-29,59-66,143-170` |
| P2 | Tempo not scraped, no TempoDown or span-rate alert; 19.5 days of silence unnoticed | no `tempo` job in `prometheus.yml`; `alerts.yml` |
| P3 | 3 dead Prometheus rules: `ContainerCrashLoop` (`container_restart_count` absent), `ZombieProcessesHigh` (no processes collector), `MemregDLQBacklog` (`memreg_dlq_depth` declared, never populated) | `/api/v1/query` 0 series each |
| P4 | 2 Loki rules firing since 2026-08-25 (`HighErrorRate`, `ContainerErrorSpike`) driven by `dhg-node-exporter` emitting 2554 error lines/5m (thermal_zone collector); severity warning so `registry/api.py:371-373` skips incident creation | Loki ruler `/prometheus/api/v1/rules` |
| P5 | `dhg-blackbox` carries `prometheus.io/scrape=true` but its compose project fails the docker-sd keep regex (`prometheus.yml:154-156`); blackbox self-metrics scraped nowhere | targets dropped list |
| P6 | Promtail config orphaned (no container, no compose service); Alloy replaced it 2026-08-25 | `config.alloy:1-2` |
| P7 | Tempo metrics_generator remote_write configured, produces nothing (`traces_*` series absent) | `tempo-config.yml:31-42` |
| P8 | Loki and Prometheus exemplar/derived-field links to Tempo are inert | datasource provisioning |
| P9 | `registry_errors_total` defined (`registry/metrics.py:33-37`), imported by ~15 modules, 0 series ever | Prometheus |
| P10 | medkb still on 10.0.0.251, UP, scraped; relocation to dh40801 has not happened; 12 `medkb_*` families declared, 0 series (no traffic) | `prometheus.yml:70-74` |
| P11 | 26 of 42 dhg-* containers have no Loki stream in 24h; Alloy has no container filter, so these containers emit nothing to stdout. `dhg-frontend` and `portage-app`: nothing in 7 days | `config.alloy:7-10` |
| P12 | `grafana-image-renderer` not installed: no alert screenshots, no PNG export | `/var/lib/grafana/plugins` empty |

---

## 6. Deprecated (LangGraph / LangSmith) inventory [A1][A2][A4]

Candidate for removal, each with what still touches it:

| # | Item | Still consumed by | Removal risk |
|---|---|---|---|
| D1 | `dhg-langgraph-traces.json` dashboard (provisioned, `dashboards.yml:12`) | nothing; renders empty | none |
| D2 | LangSmith `@traceable` in live registry: `registry/notification_service.py:22`, `registry/timeout_handler.py:18` | import only; `LANGCHAIN_TRACING_V2` unset | low; requires a code change, not an observability change |
| D3 | LangSmith `@traceable` in 17 `langgraph_workflows/dhg-agents-cloud/src/` modules + 2 Archive | no running container | none for observability; agent-code cleanup is out of this audit's scope |
| D4 | `templates/agent-boilerplate/src/agent.py:22` still scaffolds LangSmith | every new agent inherits it | must be fixed before any Pydantic AI agent is scaffolded |
| D5 | `traced_node` in `langgraph_workflows/.../tracing.py:118` (15 call sites), default endpoint `https://otel.digitalharmonyai.com/v1/traces` (Cloudflare, returns 401) | no running container | none |
| D6 | `traced_node` in `services/medkb/src/medkb/tracing.py:61` (9 call sites), env `OTEL_ENDPOINT=http://dhg-tempo:4318` | medkb is a **current** service; export has produced 0 spans (cause UNVERIFIED, 9.2) | keep the OTLP path decision open: repoint to Langfuse OTLP, keep Tempo, or both |
| D7 | Tempo + OTLP receivers + metrics_generator | only D5 and D6 | not a delete decision yet; Langfuse v3 accepts OTLP, and Pydantic AI `instrument_all()` emits OTel |
| D8 | `docker-compose.yml:31-228` 8 LangGraph agent services with `LANGCHAIN_*`/`LANGSMITH_*` env; `LANGSMITH_PROJECT`, `LANGCHAIN_API_KEY` at `:334,336` | none running | compose cleanup, outside observability tree |
| D9 | `frontend/src/app/api/langgraph/[...path]/route.ts`, `copilotkit/route.ts:11` target LangGraph Cloud, return 503 | dhg-frontend serves them | frontend scope, noted only |
| D10 | `LANGCHAIN_API_KEY` env on dhg-registry-api, dhg-medkb-api, dhg-frontend; `LOKI_URL` on registry with no reader found | nothing | vestigial |

Confirmed absent: no scrape job, alert rule, Loki rule, relabel, or datasource names langgraph/langsmith/langchain; port 2026 appears nowhere in `observability/`. [A4]

---

## 7. Gaps ranked by production risk [A5]

**CRITICAL**
- G1 Langfuse on dh40801 unmonitored: no Prometheus target on that host, `/metrics` 404, no node-exporter, nothing polls `/api/public/health`. The prior MinIO-bucket silent trace drop would recur undetected. Volumes 52GB, 81% reclaimable, on an unwatched disk.
- G2 No evidence of any Postgres backup job on g700data1 (no timer, no crontab entry), so no backup-success signal for 7 instances. (May run elsewhere, 9.5.)
- G3 `MemregDLQBacklog` can never fire: `memreg_dlq_depth` and `memreg_captures_total` declared, never populated. Every `auto-*-capture` rule depends on this pipeline.
- G4 `ContainerCrashLoop` can never fire: `container_restart_count` absent. 55 of 61 containers have no death signal at all.

**HIGH**
- G5 Registry API has no HTTP golden signals (no request rate, per-route latency, or 5xx rate anywhere).
- G6 Host-ollama-steals-:11434 still undetectable: `ollama.service` still installed; blackbox module `http_2xx` has no body match, so host ollama answers 200 identically. The prometheus.yml comment claiming coverage is wrong.
- G7 No GPU telemetry (RTX 5080 VRAM/util/temp); `gpu_utilization` queried by core-golden, 0 series; `nvidia-smi` works.
- G8 Portage has the best instrumentation in the estate (`portage_http_requests_total{route,status_code}`, duration histogram) and no dashboard, no error/latency alert.
- G9 Cloudflare tunnels unobserved: 2 systemd units, metrics on 127.0.0.1:20241, no scrape job, no external probe.
- G10 6 of 7 Postgres instances have no exporter.
- G11 Frontend :3000 and Open WebUI :3080 have no uptime probe; frontend emits no logs.

**MEDIUM**
- G12 core-golden metric-name drift (section 3). G13 medkb: rich metrics, no dashboard, no traffic alert. G14 vs-engine dashboard 5 of 7 dead. G15 `ZombieProcessesHigh` inert. G16 audio-agent exposes `/metrics` but is unscraped. G17 registry-api and session-logger scraped but session-logger has no dashboard.

**No served Pydantic AI path exists**, so agent runs, tokens, cost, tool calls, and failures are observed nowhere; this is a prerequisite gap, not a dashboard gap. [A5]

---

## 8. What is healthy. Do not rebuild. [A4][A5]

Alloy to Loki collection (unfiltered, redaction stages). cAdvisor 61/61 containers. Host memory, swap, both filesystems with alerts. registry-db Postgres exporter and dashboard data. dhg-alerting meta-dashboard. docker-overview. dhg-log-analytics. Alertmanager to registry webhook path (verified 38 deliveries). dhg-p5-loki-du textfile gauge (2.11 GiB live).

---

## 9. Contradictions and UNVERIFIED items

9.1 Target count: A4 says 17 active, A1 and A3 say 18. Likely a blackbox probe counted differently. Reviewers should settle it from `/api/v1/targets`.
9.2 Why Tempo has zero spans from medkb: never invoked vs. silent export failure. Distinguishing needs a medkb query (state change), out of read-only scope. medkb has 0 `medkb_*` series, which supports "never invoked".
9.3 Pixel rendering of both flagged dashboards was not seen by any agent (renderer absent, login gated by secret-safety). Phase 4 verification must solve login without exposing the password (e.g. a Grafana service account token stored in Doppler, or anonymous viewer role).
9.4 `docker-compose.override.yml` env keys could not be grepped (secret-safety hook); `docker inspect` on running containers was used instead, which misses keys on stopped services.
9.5 Backups may run from another user, host, or Doppler-managed job not visible to this audit.
9.6 Whether `registry_db_connections` gauge is ever set (no callers found; 24h max 1 proves occasional writes).
9.7 Rule count: A1 says 17 Prometheus rules, A4 and A5 say 18.
9.8 Alertmanager webhook history before the registry's 5-day uptime; whether the 38 POSTs created incident rows (inferred from code, DB not queried).
9.9 Langfuse worker internals (:3030 loopback-bound) and whether Portage traces are landing right now.

---

## 10. Phase 1 status

Complete. Next: STOP GATE. Adversarial review (4 Opus 5 agents) launches only on explicit permission.

## Version history
- v1 2026-09-04: initial synthesis from five audit agents.
