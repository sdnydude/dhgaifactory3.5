# DHG Audio Analysis Agent — Environment Variables
# Per Build Spec Section 6.3
# Copy to .env and fill in values

# =============================================================================
# OLLAMA LLM CONFIGURATION
# =============================================================================

# Ollama API endpoint (Docker network or localhost)
OLLAMA_BASE_URL=http://ollama:11434

# Ollama model for translation/summarization/tagging
OLLAMA_MODEL=llama3.1:8b-instruct-q4_K_M

# Timeout for Ollama API calls (seconds)
OLLAMA_TIMEOUT=120

# =============================================================================
# WHISPER CONFIGURATION
# =============================================================================

# Whisper model size (tiny, base, small, medium, large-v3)
WHISPER_MODEL_SIZE=large-v3

# Device for Whisper inference (cuda or cpu)
WHISPER_DEVICE=cuda

# Compute type (int8 for GPU, float32 for CPU)
WHISPER_COMPUTE_TYPE=int8

# =============================================================================
# HUGGINGFACE (for pyannote diarization)
# =============================================================================

# Free HuggingFace token for pyannote model download
# Get from: https://huggingface.co/settings/tokens
# Required for speaker diarization
HF_TOKEN=

# =============================================================================
# POSTGRESQL DATABASE
# =============================================================================

POSTGRES_USER=user
POSTGRES_PASSWORD=pass
POSTGRES_DB=audio_agent

# Full connection URL (constructed from above)
# POSTGRES_URL=postgresql+asyncpg://user:pass@postgres:5432/audio_agent

# =============================================================================
# FILE STORAGE
# =============================================================================

# Directory for audio file uploads (mounted volume)
AUDIO_UPLOAD_DIR=/data/audio

# =============================================================================
# LOGGING
# =============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
